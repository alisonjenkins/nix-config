{
  # ALVR disabled — WiVRn is the active streaming solution; having both
  # enabled causes unnecessary open firewall ports and runtime confusion.
  # programs.alvr = {
  #   enable = true;
  #   openFirewall = true;
  # };
}
